//creates variables
let canvas = document.getElementById("myCanvas");
let c = canvas.getContext("2d");

// declare variables W and H = width and height of canvas

// flip Coin function
function flipCoin() {

    // declare variable flip; assign random number: 0 - 14
        // 0 - heads, 1 - tails
}